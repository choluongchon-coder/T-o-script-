-- ==========================================
-- TỰ ĐỘNG CHỌN PHE HẢI QUÂN & SIÊU GIẢM LAG
-- ==========================================
pcall(function()
    game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("SetTeam", "Marines")
    settings().Rendering.QualityLevel = 1
    game:GetService("Lighting").GlobalShadows = false
    for _, v in pairs(game:GetDescendants()) do
        if v:IsA("Part") or v:IsA("MeshPart") then
            v.Material = Enum.Material.SmoothPlastic
        elseif v:IsA("Decal") or v:IsA("Texture") or v:IsA("ParticleEmitter") then
            v:Destroy()
        end
    end
end)

-- ==========================================
-- GIAO DIỆN CỐ ĐỊNH (KHÔNG THỂ DI CHUYỂN)
-- ==========================================
local ScreenGui = Instance.new("ScreenGui")
local MainFrame = Instance.new("Frame")
local UIStroke = Instance.new("UIStroke")
local Title = Instance.new("TextLabel")
local TimeLabel = Instance.new("TextLabel")
local StatusLabel = Instance.new("TextLabel")

ScreenGui.Parent = game:GetService("CoreGui")
ScreenGui.Name = "HydraDat_ChestHub"

MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 15) 
MainFrame.Position = UDim2.new(0.02, 0, 0.1, 0)
MainFrame.Size = UDim2.new(0, 340, 0, 160) 
MainFrame.Active = false
MainFrame.Draggable = false 

UIStroke.Parent = MainFrame
UIStroke.Color = Color3.fromRGB(255, 0, 0) 
UIStroke.Thickness = 3

Title.Parent = MainFrame
Title.BackgroundTransparency = 1
Title.Position = UDim2.new(0, 0, 0.05, 0)
Title.Size = UDim2.new(1, 0, 0.25, 0)
Title.Font = Enum.Font.SourceSansBold
Title.Text = "🔥 tiktok:hydradat 🔥"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 24

StatusLabel.Parent = MainFrame
StatusLabel.BackgroundTransparency = 1
StatusLabel.Position = UDim2.new(0, 10, 0.35, 0)
StatusLabel.Size = UDim2.new(1, -20, 0.25, 0)
StatusLabel.Font = Enum.Font.SourceSansBold
StatusLabel.Text = "Trạng thái: Gom rương gần -> xa..."
StatusLabel.TextColor3 = Color3.fromRGB(0, 255, 0)
StatusLabel.TextSize = 15
StatusLabel.TextXAlignment = Enum.TextXAlignment.Left

TimeLabel.Parent = MainFrame
TimeLabel.BackgroundTransparency = 1
TimeLabel.Position = UDim2.new(0, 10, 0.65, 0)
TimeLabel.Size = UDim2.new(1, -20, 0.25, 0)
TimeLabel.Font = Enum.Font.SourceSans
TimeLabel.Text = "Thời gian còn lại ở SV này: 236s"
TimeLabel.TextColor3 = Color3.fromRGB(255, 50, 50)
TimeLabel.TextSize = 17
TimeLabel.TextXAlignment = Enum.TextXAlignment.Left

-- ==========================================
-- CÁC BIẾN KIỂM SOÁT HỆ THỐNG
-- ==========================================
_G.AutoChest = true
_G.PauseHop = false 
_G.FastAttack = false -- Biến kiểm soát tốc độ đánh nhanh
local timeLeft = 236 

local TweenService = game:GetService("TweenService")
function TweenTo(targetCFrame)
    local char = game.Players.LocalPlayer.Character
    if not char or not char:FindFirstChild("HumanoidRootPart") then return end
    if char.HumanoidRootPart.Anchored then char.HumanoidRootPart.Anchored = false end
    
    local distance = (targetCFrame.Position - char.HumanoidRootPart.Position).Magnitude
    local speed = 300 
    local tweenInfo = TweenInfo.new(distance / speed, Enum.EasingStyle.Linear)
    
    local tween = TweenService:Create(char.HumanoidRootPart, tweenInfo, {CFrame = targetCFrame})
    tween:Play()
    tween.Completed:Wait()
end

function EquipWeapon()
    local lp = game.Players.LocalPlayer
    for _, tool in pairs(lp.Backpack:GetChildren()) do
        if tool:IsA("Tool") and (tool.ToolTip == "Melee" or tool.ToolTip == "Sword") then
            lp.Character.Humanoid:EquipTool(tool)
            break
        end
    end
end

function SetAntiGravity(char)
    if not char.HumanoidRootPart:FindFirstChild("BodyVelocity") then
        local bv = Instance.new("BodyVelocity")
        bv.Velocity = Vector3.new(0, 0, 0)
        bv.MaxForce = Vector3.new(9e9, 9e9, 9e9)
        bv.Parent = char.HumanoidRootPart
    end
    char.HumanoidRootPart.CanCollide = false
    char.Humanoid.PlatformStand = true
end

-- ==========================================
-- VÒNG LẶP FAST ATTACK (TĂNG TỐC ĐÁNH x1.5)
-- ==========================================
task.spawn(function()
    while true do
        task.wait() -- Vòng lặp chạy ở tần suất mili-giây siêu tốc
        if _G.FastAttack then
            pcall(function()
                local VirtualUser = game:GetService('VirtualUser')
                VirtualUser:CaptureController()
                VirtualUser:ClickButton1(Vector2.new(850, 520))
                -- Gửi thêm tín hiệu kích hoạt chém của vũ khí để nhân sát thương nhanh hơn
                local weapon = game.Players.LocalPlayer.Character:FindFirstChildOfClass("Tool")
                if weapon then
                    weapon:Activate()
                end
            end)
        end
    end
end)

-- ==========================================
-- HỆ THỐNG KHÓA MỤC TIÊU VÀ DIỆT BOSS TRÊN ĐẦU
-- ==========================================
spawn(function()
    while true do
        task.wait(0.1) 
        pcall(function()
            local lp = game.Players.LocalPlayer
            local char = lp.Character
            local mySea = game.PlaceId
            
            -- --------------------------------------
            -- KHU VỰC SEA 2: EVENT RÂU ĐEN
            -- --------------------------------------
            if mySea == 2753915549 then 
                local hasDarkFist = lp.Backpack:FindFirstChild("Fist of Darkness") or char:FindFirstChild("Fist of Darkness")
                
                if hasDarkFist then
                    _G.PauseHop = true
                    _G.AutoChest = false
                    
                    local boss = game.Workspace.Enemies:FindFirstChild("Darkbeard")
                    if not boss then
                        _G.FastAttack = false -- Tắt đánh nhanh khi đang chạy di chuyển
                        StatusLabel.Text = "🚨 ĐANG ĐI TRIỆU HỒI RÂU ĐEN..."
                        StatusLabel.TextColor3 = Color3.fromRGB(255, 100, 0)
                        if lp.Backpack:FindFirstChild("Fist of Darkness") then
                            char.Humanoid:EquipTool(lp.Backpack["Fist of Darkness"])
                        end
                        TweenTo(CFrame.new(-3715, 16, -5150))
                    else
                        StatusLabel.Text = "⚔️ ĐANG GHÌ ĐẦU DIỆT RÂU ĐEN (FAST x1.5)..."
                        StatusLabel.TextColor3 = Color3.fromRGB(255, 0, 0)
                        SetAntiGravity(char)
                        
                        char.HumanoidRootPart.CFrame = boss.HumanoidRootPart.CFrame * CFrame.new(0, 11, 0)
                        EquipWeapon()
                        _G.FastAttack = true -- BẬT SIÊU TỐC ĐỘ ĐÁNH
                    end
                else
                    _G.FastAttack = false
                end
                
            -- --------------------------------------
            -- KHU VỰC SEA 3: EVENT KATAKURI V2
            -- --------------------------------------
            elseif mySea == 7449423635 then 
                local hasChalice = lp.Backpack:FindFirstChild("God's Chalice") or char:FindFirstChild("God's Chalice")
                local hasSweet = lp.Backpack:FindFirstChild("Sweet Chalice") or char:FindFirstChild("Sweet Chalice")
                
                if hasChalice or hasSweet then
                    _G.PauseHop = true
                    _G.AutoChest = false
                    
                    if hasChalice and not hasSweet then
                        _G.FastAttack = false
                        local cocoaCount = 0
                        local cocoa = lp.Backpack:FindFirstChild("Conjured Cocoa") or char:FindFirstChild("Conjured Cocoa")
                        if cocoa then cocoaCount = cocoa.Value end
                        
                        if cocoaCount < 10 then
                            StatusLabel.Text = "🍫 ĐANG FARM BỘT CACAO ("..cocoaCount.."/10)..."
                            StatusLabel.TextColor3 = Color3.fromRGB(255, 150, 150)
                            
                            for _, enemy in pairs(game.Workspace.Enemies:GetChildren()) do
                                if (string.find(enemy.Name, "Cocoa") or string.find(enemy.Name, "Chocolate")) and enemy:FindFirstChild("HumanoidRootPart") and enemy.Humanoid.Health > 0 then
                                    SetAntiGravity(char)
                                    char.HumanoidRootPart.CFrame = enemy.HumanoidRootPart.CFrame * CFrame.new(0, 11, 0)
                                    EquipWeapon()
                                    _G.FastAttack = true -- Bật chém nhanh farm quái thường
                                    break
                                end
                            end
                        else
                            _G.FastAttack = false
                            StatusLabel.Text = "🥛 ĐANG ĐỔI CHÉN NGỌT (SWEET CHALICE)..."
                            TweenTo(CFrame.new(-2125, 68, -12122))
                            game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("SweetCrafter", "Exchange")
                        end
                        
                    elseif hasSweet then
                        local killCount = game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("CakeLandProgress") or 0
                        local boss = game.Workspace.Enemies:FindFirstChild("Dough King") or game.Workspace.Enemies:FindFirstChild("Cake Prince")
                        
                        if killCount < 500 and not boss then
                            StatusLabel.Text = "🍰 ĐANG FARM ĐỦ 500 QUÁI KATA ("..killCount.."/500)..."
                            StatusLabel.TextColor3 = Color3.fromRGB(255, 200, 0)
                            
                            for _, enemy in pairs(game.Workspace.Enemies:GetChildren()) do
                                if enemy.Parent.Name == "Enemies" and enemy:FindFirstChild("HumanoidRootPart") and enemy.Humanoid.Health > 0 then
                                    SetAntiGravity(char)
                                    char.HumanoidRootPart.CFrame = enemy.HumanoidRootPart.CFrame * CFrame.new(0, 11, 0)
                                    EquipWeapon()
                                    _G.FastAttack = true -- Bật chém nhanh gom quái hòn đảo
                                    break
                                end
                            end
                        else
                            if not boss then
                                _G.FastAttack = false
                                StatusLabel.Text = "🔮 ĐANG DI CHUYỂN TRIỆU HỒI KATAKURI V2..."
                                if lp.Backpack:FindFirstChild("Sweet Chalice") then
                                    char.Humanoid:EquipTool(lp.Backpack["Sweet Chalice"])
                                end
                                TweenTo(CFrame.new(-2320, 68, -12150))
                            else
                                StatusLabel.Text = "⚔️ ĐANG GHÌ ĐẦU DIỆT KATAKURI CHUẨN V2 (FAST x1.5)!"
                                StatusLabel.TextColor3 = Color3.fromRGB(255, 0, 0)
                                SetAntiGravity(char)
                                
                                char.HumanoidRootPart.CFrame = boss.HumanoidRootPart.CFrame * CFrame.new(0, 11, 0)
                                EquipWeapon()
                                _G.FastAttack = true -- BẬT SIÊU TỐC ĐỘ ĐÁNH KHI ĐẤU BOSS LỚN
                            end
                        end
                    end
                else
                    _G.FastAttack = false
                end
            end
        end)
    end
end)

-- ==========================================
-- VÒNG LẶP QUÉT VÀ BAY ĐI NHẶT RƯƠNG THÔNG MINH
-- ==========================================
spawn(function()
    while true do
        task.wait(0.5)
        if _G.AutoChest and not _G.PauseHop then
            pcall(function()
                local lp = game.Players.LocalPlayer
                local char = lp.Character
                if char and char:FindFirstChild("HumanoidRootPart") and char:FindFirstChildOfClass("Humanoid") then
                    
                    local chestTable = {}
                    for _, v in pairs(game.Workspace:GetDescendants()) do
                        if string.find(v.Name, "Chest") and v:IsA("Part") then
                            table.insert(chestTable, v)
                        end
                    end
                    
                    table.sort(chestTable, function(a, b)
                        local distA = (a.Position - char.HumanoidRootPart.Position).Magnitude
                        local distB = (b.Position - char.HumanoidRootPart.Position).Magnitude
                        return distA < distB 
                    end)
                    
                    if #chestTable > 0 then
                        StatusLabel.Text = "Trạng thái: Gom rương gần -> xa ("..#chestTable.." cái)"
                        StatusLabel.TextColor3 = Color3.fromRGB(0, 255, 0)
                        for _, chest in pairs(chestTable) do
                            if chest and chest.Parent and _G.AutoChest and not _G.PauseHop then
                                
                                SetAntiGravity(char)
                                TweenTo(chest.CFrame)
                                task.wait(0.15)
                            end
                        end
                    else
                        StatusLabel.Text = "Trạng thái: Hết rương, chờ nhảy phòng..."
                        StatusLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
                        char.Humanoid.PlatformStand = false
                        if char.HumanoidRootPart:FindFirstChild("BodyVelocity") then
                            char.HumanoidRootPart.BodyVelocity:Destroy()
                        end
                    end
                end
            end)
        end
    end
end)

-- ==========================================
-- ĐẾM NGƯỢC THỜI GIAN & TỰ ĐỘNG ĐỔI SERVER
-- ==========================================
spawn(function()
    while true do
        task.wait(1)
        if not _G.PauseHop then
            if timeLeft > 0 then
                timeLeft = timeLeft - 1
                TimeLabel.Text = "Thời gian còn lại ở SV này: " .. tostring(timeLeft) .. "s"
            else
                TimeLabel.Text = "🚀 ĐANG HOP SERVER MỚI..."
                task.wait(0.2)
                
                local HttpService = game:GetService("HttpService")
                local TeleportService = game:GetService("TeleportService")
                local PlaceId = game.PlaceId
                
                pcall(function()
                    local servers = HttpService:JSONDecode(game:HttpGet("https://games.roblox.com/v1/games/" .. PlaceId .. "/servers/Public?sortOrder=Asc&limit=100"))
                    for _, server in pairs(servers.data) do
                        if server.playing < server.maxPlayers and server.id ~= game.JobId then
                            TeleportService:TeleportToPlaceInstance(PlaceId, server.id, game.Players.LocalPlayer)
                            break
                        end
                    end
                end)
            end
        else
            TimeLabel.Text = "⏸️ ĐÃ KHÓA HOP (Đang làm Event Boss)"
        end
    end
end)
